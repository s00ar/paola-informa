rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permisos amplios: cualquier usuario autenticado puede leer y escribir.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
